package ui.theme

import androidx.compose.ui.graphics.Brush
import androidx.compose.ui.graphics.Color

val SublimeLight = Brush.linearGradient(
    colors = listOf(
        Color(0xFFFC5C7D),
        Color(0xFF6A82FB)
    )
)

val SeaWeed = Brush.linearGradient(
    colors = listOf(
        Color(0xFF4CB8C4),
        Color(0xFF3CD3AD)
    )
)

val Stripe = Brush.linearGradient(
    colors = listOf(
        Color(0xFF1FA2FF),
        Color(0xFF12D8FA),
        Color(0xFFA6FFCB)
    )
)

val Lizard = Brush.linearGradient(
    colors = listOf(
        Color(0xFF304352),
        Color(0xFFd7d2cc)
    )
)